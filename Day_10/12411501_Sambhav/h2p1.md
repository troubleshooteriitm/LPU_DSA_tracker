#Reverse Linked List

link -https://leetcode.com/problems/reverse-linked-list/description/

```python 


class Solution:
    def reverseList(self, head):
        prev = None
        curr = head

        while curr:
            nxt = curr.next      
            curr.next = prev     
            prev = curr          
            curr = nxt           

        return prev

```