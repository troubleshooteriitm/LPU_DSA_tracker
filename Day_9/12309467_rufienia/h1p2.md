# palindrome number
- https://leetcode.com/problems/palindrome-number/description/

```
pyton 
class Solution:
    def isPalindrome(self, x: int) -> bool:
        s=str(x)
        if s == s[::-1]:
            return True
        else:
            return False
```